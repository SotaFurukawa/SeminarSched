# 変更履歴

この文書は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) の考え方に沿って、
利用者に影響する変更を記録する。バージョン番号は Semantic Versioning に従う。
v1系はすべてBeta版として扱い、正式リリースを明示的に決定した場合にv2系へ移行する。

## [Unreleased]

### 追加

- Googleフォーム作成手順の左下から、同じ10手順を独立したウィンドウで表示できる
  操作を追加した。
- ③アンケート取込みに、学年で絞り込んだ複数生徒の参加可否を、開校日・コマ単位で
  一括編集する画面を追加した。

### セキュリティ・品質

- 生徒参加可否の手動変更を1トランザクションで保存し、氏名や外部IDを含めない
  監査ログを記録するようにした。休校日や無効なコマへの変更は拒否する。

## [1.4.3] - 2026-08-31

### 変更

- ①設定のプロジェクト編集でも、講習開始日・終了日を2020年から2070年までの
  年・月・日プルダウンで選択できるようにした。
- ウィンドウタイトル、未選択時のヘッダー、About画面、OS上のアプリ表示名に、
  `v1.4.3 (Beta)`を含むソフト名を表示するようにした。
- Googleフォーム作成ガイドを、作成キットの保存から回答用リンクを開くまでの10手順へ
  統合し、利用者提供の実画面スクリーンショットを各手順の直下に表示するようにした。
- アンケート作成画面と画像ガイドに分かれていた説明を一本化し、「赤い案内つき」などの
  不要な表現と旧合成画像を削除した。

### セキュリティ・品質

- ガイドへ収録するスクリーンショットのローカルパス、メールアドレス、生成済みURLを
  不透明な黒塗りで伏せ、個人情報を含み得る原本は配布パッケージから除外した。
- 10手順の順序、画像参照、配布パッケージへの画像同梱を回帰テストの対象にした。

## [1.4.2] - 2026-08-30

### 追加

- 事前確定で個別指導と集団授業を選べるようにし、個別指導は学年・生徒・科目・講師・
  日付・コマを順に選択できるようにした。
- 集団授業の講師・日付・コマを事前確定し、同じ講師へ個別指導を重複配置しない
  ハード制約として扱えるようにした。

### 変更

- 共通基本情報Excelを反映するとき、正本をworkspaceへコピーし、以前の正本を
  `生徒・講師_基本情報(by日時).xlsx`としてバックアップするようにした。
- 共通基本情報Excelの在籍欄をMicrosoft Excelのセル内チェックボックスにし、
  生徒ID・講師IDは未使用の最小番号を割り当てるようにした。
- 設定画面の科目一覧と編集欄の間隔を調整した。

### セキュリティ・品質

- Googleフォーム作成ガイドのスクリーンショットに含まれる氏名・アカウント識別子を、
  復元できない不透明な黒塗りへ差し替えた。
- 事前確定、共通基本情報の世代バックアップ、Excelチェックボックス、未使用ID採番、
  手順1の完了条件に回帰テストを追加した。

## [1.4.1] - 2026-08-29

### 追加

- 新規プロジェクトの開始日・終了日とアンケート回答締切を、年・月・日の選択式にした。
  年度は2020年から2070年まで選択できる。
- Google Apps Scriptの貼付け、実行、初回権限確認、回答CSVの保存を、赤枠と矢印の
  画面イメージで案内するアプリ内ガイドを追加した。

### 変更

- ホームでは反映済みの共通基本情報を開く操作を大きな青ボタンとし、新規作成と反映を
  その下の白いボタンへ整理した。
- 設定タブは選択中を青、未選択を白で明示し、コマ一覧と編集欄の間隔を広げた。
- アンケート取込みを生徒回答・講師回答の一括操作へ集約し、Google SheetsからCSVを
  保存する手順を画面に表示した。
- ホームの縦スクロール範囲を明示し、常時表示のスクロールバーから④以降へ移動できる
  ようにした。

### 品質

- Googleフォームのチェックボックス表が`Z, A, B, C`を1セルへ保存する実形式を
  回帰テストし、各コマの不可回答へ正しく分解できることを確認した。

## [1.4.0] - 2026-08-29

### 追加

- 業務フローを①設定、②アンケート作成、③アンケート取込み、④事前確定、⑤時間割、
  ⑥出力へ整理し、アンケート作成と取込みを別画面にした。
- 保護者等と調整済みの生徒・講師・日付・コマを、ハード制約検証済みのロック枠として
  最適化前に登録できる「事前確定」を追加した。
- ホームへ共通基本情報Excelの新規保存、取込み、反映済み正本を開く操作を集約した。

### 変更

- 年度と春期／夏期／冬期の選択から、`2026夏期講習`形式のプロジェクト名を自動設定する。
- 共通基本情報Excelの通常授業と講師対応科目は、氏名選択からIDを設定し、登録済み行は
  安定IDから最新の生徒名・講師名・科目名へ自動追従する。
- 通常担当優先度の既定値を`3`、1対1必須を`いいえ`、指導可能を`はい`とした。
- v1系をすべてBeta版として扱い、正式リリースの明示があるまではv1系とGitHubの
  Pre-release運用を継続する。

### 互換性・品質

- DB schemaと`.jukuschedule`は変更せず、旧8列の講師対応科目と旧12列の通常授業も
  引き続き読み込める。
- 事前確定は既存のハード制約、AuditLog、Undo／Redo、ロック経路を再利用する。

## [1.3.4] - 2026-08-22

### 修正

- `生徒・講師_基本情報.xlsx`で複数人を追加したとき、全員へ同じ`S-0001`／`T-0001`が
  表示される問題を修正した。既存IDを保持し、未使用番号を入力順に割り当てる。
- 退籍にした入力済み行へ、行全体を灰色にする条件付き書式が正しく適用されるようにした。

### 変更

- 生徒・講師シートA列の文字による在籍選択を、Microsoft 365／Excel 2024以降で
  ワンクリックできるセル内チェックボックスへ変更した。行内には在籍・退籍の文字を置かない。
- 古いExcelの`TRUE`／`FALSE`表示と、従来の`☑ 在籍`／`☐ 退籍`形式は引き続き読み込める。

### 品質

- 複数行の連番、手入力ID保持、セル内チェックボックス構造、退籍値、灰色条件式を
  回帰テストし、Microsoft Excelでも数式計算と表示を確認した。

## [1.3.3] - 2026-08-22

### 変更

- 共通名簿の在籍列をA列へ移動し、姓入力時のID候補、在籍、最大連続2、空きコマなしを
  入力補助として表示した。
- 必須入力セルを薄い黄色にし、ヘッダーへ既定値を明記した。
- アプリ反映時にプロジェクトを含む既存IDとの衝突を確認し、正式IDを書き戻すようにした。

### 互換性

- v1.3.2以前の列順と文字による在籍値を引き続き読み込める。

## [1.3.2] - 2026-08-22

### 変更

- Excelに表示・入力する生徒学年を、小学校`S1`～`S6`、中学校`J1`～`J3`、
  高校`H1`～`H3`へ統一した。
- 共通名簿、マスターデータ、アンケート統合結果、参照シート、集団授業テンプレート、
  時間割Excel帳票へ同じ学年表記を適用した。

### 互換性

- DB、既存`.jukuschedule`、画面、Googleフォームの日本語学年表記は変更しない。
- Excel取込みでは新しいS/J/H表記に加えて、従来の`小2`、`中学3年`、`高校1年`等も
  引き続き受け付け、内部の短縮表記へ安全に正規化する。

### 品質

- 学年変換、旧表記互換、各Excel経路、アンケート原本保持、Excel帳票を回帰テストした。

## [1.3.1] - 2026-08-22

### 追加

- 小学校・中学校・高校の全26科目から現在の指導可能科目を複数選択できる講師用
  Googleフォーム作成スクリプトを追加した。講師メールは収集せず、回答原本は
  Google Sheetsへ保存し、確認後に共通の講師マスターへ反映する運用とした。
- 生徒・講師の新規登録時に、利用者がIDを入力しなくても`S-0001`／`T-0001`形式で
  未使用の連番を安全に自動採番するようにした。既存IDは変更しない。

### 変更

- 小学校の算数・国語をそれぞれ「中学受験」と「中学受験以外なら可能」に分け、
  高校数学を「数学IA」「数学IIBC」「数学III」の3区分にした。
- 既存データの参照を保つため、旧算数・国語は中学受験以外、旧数学一般は数学IAへ
  安全に移行する。新設科目の講師資格は推定せず、明示設定を必要とする。

### 品質

- 旧科目IDと講師資格を保持するDB migration往復テスト、26科目のExcel・Googleフォーム
  契約テスト、自動IDの重複防止テストを追加した。

## [1.3.0] - 2026-08-18

### 追加

- 講習に依存しない生徒・講師・科目・指導可能科目・通常授業を、ホームから開ける
  `生徒・講師_基本情報.xlsx`で一元管理できるようにした。
- 生徒用と講師用のGoogleフォーム回答を2ファイル同時に選び、氏名照合、受講希望、
  不可時間、要確認事項を一括で検証・反映する推奨フローを追加した。
- 回答原本、正規化データ、赤・黄の要確認一覧を含む統合xlsxを`.jukuschedule`内へ保存し、
  必要なときに外部へ書き出せるようにした。
- 未登録の生徒回答を、要確認一覧から講習内だけの体験生として解決できるようにした。
- 通常授業担当、担当優先度、1対1必須を講習別受講回数から分離するDB migrationを追加した。

### 変更

- 新規講習は共通名簿を自動反映し、既存講習はホームの明示操作時だけ同期するようにした。
- 退籍した生徒・講師を削除せず、共通名簿の末尾へ灰色で保持するようにした。
- 講師負荷の最終ソフト目的を、単純な担当数差から勤務可能枠に対する参加割合差へ変更した。
  未配置最小化などの上位目的と、すべてのハード制約は変更していない。

### 品質

- DB migrationの往復、共通名簿、一括アンケート、体験生解消、講師参加割合の回帰テストを
  追加し、全440テストを通過した。

## [1.2.0] - 2026-08-12

### 追加

- 開校日・休校日カレンダーで複数日をチェックし、選択日をまとめて開校または休校へ
  変更できる一括設定を追加した。
- アンケート取込み画面から、現在の開校日・有効なコマ・科目を反映した生徒用／講師用
  Googleフォーム作成Apps Scriptと手順書を一括出力できるようにした。
- Googleフォーム作成キットの保存からApps Scriptの実行、URL確認までを画面イメージで
  案内するポップアップを追加した。

### 変更

- アンケート取込み画面の「生徒回答」「講師回答」を同じ大きさと表示方式に統一した。

## [1.1.0] - 2026-08-11

### 追加

- 業務順序を常時確認できる4段階ホームとサイドバー、実データに基づく進捗・次操作を
  追加した。
- 生徒・講師の初期Excel一括登録と、日常的な少人数追加向け3段階フォームを追加した。
- 集団授業の週カレンダー、未配置・時間割・選択詳細の3ペイン編集画面を追加した。
- 警告から修正対象画面へ移動する導線と、出力後に保存先フォルダーを開く機能を追加した。

### 変更

- アンケート取込みを「回答ファイル選択→内容確認→反映完了」の順に整理し、通常不要な
  列設定を折りたたんだ。
- 出力画面を「出力対象→形式→保存先」の順に整理し、詳細な帳票設定を折りたたんだ。
- 背景、surface、境界、状態色、余白、文字、buttonを共通QML theme/componentへ統一した。

### 品質

- DB、migration、OR-Tools制約、DnD二重検証、lock、Undo/Redo、AuditLog、Excel/PDF
  rendererを変更せず、UI導線と日本語パスの回帰testを追加した。

## [1.0.4] - 2026-08-07

### 修正

- `v1.0.3`のGitHub Actionsで検出されたRuff書式差分を修正し、履歴を
  書き換えずに再ビルドできるリリースとした。
- Alembic 1.19系で監査ログのCHECK制約名が二重化する差異を修正し、
  1.18系と1.19系のどちらでも同じDBスキーマ名になるようにした。

## [1.0.3] - 2026-08-07

### 追加

- ホームに「授業日→アンケート→時間割配置→個人時間割出力」の4段階フローを追加した。
- 利用者別の共通workspaceへ生徒・講師・プロジェクトフォルダを自動作成し、新規
  プロジェクトの保存先と既存プロジェクト選択の初期位置を自動化した。
- 反映済みの生徒・講師アンケート原本を`.jukuschedule`内へ保持し、再取込み時に
  原本snapshotを差し替えるマイグレーションを追加した。
- 集団授業を開校日、学年、科目、コマ、講師から画面登録できるカレンダー導線を追加した。
- 画像で確認した生徒アンケート構成に合わせ、氏名、学年、在籍区分、最大4科目の
  受講回数、受講不可日時、特記事項、学力テストを収集するGoogleフォームと回答原本
  スプレッドシートの自動作成スクリプトを追加した。
- フォームの科目選択肢をアプリ既定の23科目と一致させ、高校社会を日本史・世界史・
  地理・政治経済に分離した。
- 講師の姓・名、出勤不可日時、確認、特記事項を収集する講師用Googleフォームと
  回答原本スプレッドシートの自動作成スクリプトを追加した。
- 生徒・講師フォームの氏名を姓と名に分け、時間割表では姓が一意なら姓のみ、同姓が
  複数ならフルネームを表示するようにした。

### 変更

- 講師用Googleフォームではメールアドレスを収集しないようにした。
- 新規プロジェクトでは校舎名と保存先の入力を不要にし、現在のプロジェクトから生徒・
  講師・講師対応科目を引き継げるようにした。卒業・退会・休止者は末尾へ並べて灰色表示する。
- 生徒アンケートは学年回答に応じて小学校・中学校・高校の科目セクションへ分岐し、
  回答者の校種に該当する科目だけを表示するようにした。
- SignPath Foundation申請の不承認を受け、社内利用向け成果物を意図的に未署名で配布する
  方針へ変更した。リリースノートとチェックリストから署名要求手順を除き、未署名境界と
  SHA-256の検証を必須とした。

### 変更

- アンケートテンプレートへ生徒・講師・科目の参照シート、ID選択、名前・科目名の
  確認機能を追加した。マスター更新とアンケート反映の安全な処理境界は維持する。
- マスターExcelの学年を小1～高3の選択式にし、入力必須列をヘッダーで明示した。
- 指導可能は「はい」、担当講師優先度は3、1対1必須は「いいえ」を空欄時の
  既定値とした。

## [1.0.2] - 2026-08-01

### 修正

- GitHub Windowsランナーに初期導入された`filelock`を固定リリース環境の構築前に
  除去し、意図しないパッケージを拒否する環境検証を維持したままビルドできるようにした。

## [1.0.1] - 2026-08-01

### 変更

- マスターExcelで生徒の標準最大連続コマ数、空きコマ許可、有効などを空欄にした
  場合の安全な既定値を追加した。
- 講師対応科目と受講希望に、ID・科目コードから表示名を確認する列と、名前の
  プルダウンから正式なID・コードを選ぶ入力補助列を追加した。

### ドキュメント

- 初めての利用者向け簡単操作ガイドを追加した。
- Googleフォームの質問例、推奨設定、回答をアプリ用の縦長形式へ変換する手順、
  希望講師を変更しない場合の安全な列マッピング方法を追加した。
- 架空生徒50名・架空講師20名を含み、通常担当と希望講師の科目資格を検証する
  `master_data.xlsx`生成ツールと利用手順を追加した。

## [1.0.0-rc.4] - 2026-07-30

### 修正

- Alembicが起動時に参照するSQLAlchemy方言を残し、MSVCのメモリ不足原因だった
  `sqlalchemy.dialects.oracle.dictionary`だけをNuitka対象外にした。
- packaged smoke失敗時に、ローカルログがあればActionsへ表示し、ログ初期化前の
  失敗も明示するようにした。

## [1.0.0-rc.3] - 2026-07-29

### 修正

- NuitkaのWindows buildでSQLite以外の未使用SQLAlchemy方言をコンパイル対象外とし、
  SQLite方言を明示包含した。
- C compilerの並列job数を2へ制限し、GitHub-hosted runnerでMSVCが
  `C1002: compiler is out of heap space`になる可能性を抑えた。

## [1.0.0-rc.2] - 2026-07-29

### 修正

- GitHub-hosted Windows runnerに既定導入された補助パッケージをrelease build前に
  除去し、固定した配布依存だけを許可する環境検証が誤検知しないようにした。

### 追加

- SignPath Foundation申請に必要なCode signing policy、プライバシーポリシー、
  役割、申請・有効化手順、Authenticode検証scriptを追加。
- プロジェクト自身はGPL-3.0-only。第三者コンポーネントの公開前確認事項は
  [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md) を参照。

## [1.0.0-rc.1] - 2026-07-29

初回リリース候補。これは最終版の公開を意味せず、受入確認と配布承認のための候補である。

### 追加

- Windows 10 / 11 x64向けの日本語PySide6 / QMLデスクトップUI。
- 講習ごとの単一SQLiteプロジェクトファイル（`.jukuschedule`）。
- プロジェクト、コマ、開校日、科目、生徒、講師、講師対応科目、受講希望の管理。
- マスターExcel、アンケートxlsx / CSV、集団授業xlsxの取込み・検証・差分確認。
- OR-Tools CP-SATによる、未配置を許容しつつハード制約を破らない辞書式最適化。
- 時間割グリッド、手動編集、即時検証、授業単位ロック、Undo / Redo、監査ログ、
  ロックを保持する全体再最適化。
- 全体・生徒別・講師別・未配置／警告のExcel / PDF、割当て生データCSV、
  PDF印刷プレビュー。
- アプリ版とDBスキーマ版を区別するAbout表示および帳票のアプリ版表示。
- Windowsポータブル版、インストーラー、SHA-256一覧を作るための配布基盤。
- CIの品質検査と、タグを入力とするWindows配布ワークフロー。
- 利用者マニュアル、トラブルシューティング、性能記録、受入表、リリースチェックリスト。

### 安全性

- QML、業務ロジック、DB、最適化、取込み、出力を分離。
- 最適化結果と手動編集を独立validatorで再検査し、ハード制約違反を保存・出力しない。
- プロジェクト複製と帳票出力は一時ファイル経由で成功後だけ置換。
- migration前バックアップ、手動バックアップ、監査ログ、ローカル技術ログ。
- テレメトリ、クラウドDB、Google API必須連携を含まない完全ローカル動作。

### 既知の制限

- 本番GitHub Release、タグ、署名済み成果物は作成していない。
- 署名なし実行ファイルはWindows SmartScreenの警告対象になり得る。
- 同一build machineでのfresh install／uninstallは確認済みだが、クリーンな別Windows
  PC、上書きupgrade、offline、実プリンター、OneDrive同期競合、長時間GUI操作は、
  実施結果が受入表で `PASS` になるまで保証しない。
- 最深部がおよそ264文字になる過長なinstall pathではinstallerがrollbackした。
  通常の短い日本語pathは確認済みだが、OS上限近傍のpath対応は保証しない。
- 選択範囲だけの部分再最適化、一括ロック、再起動をまたぐUndo / Redoは未実装。
- 指定参考PDFが不在のため、実帳票との視覚的な直接比較は未実施。
- Qt Community EditionのGPLv3互換性、対応ソース、第三者noticeは完成artifactに対して
  公開前の最終監査が必要。
